@model IEnumerable<GRP.AppWeb.Models.Articulo>

<table class="table">
    <tr>
        <th></th>
        <th>Ingrediente</th>
        <th>U.M</th>
        <th>Costo</th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td><input type="radio" name="rbMedico" value="@item.Id"></td>
        <td>
            @Html.DisplayFor(modelItem => item.Descripcion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.UnidadMedida)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CostoxUM)
        </td>
    </tr>
}

</table>
